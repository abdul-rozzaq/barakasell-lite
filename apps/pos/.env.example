# Baked into the browser bundle at build time (Vite import.meta.env.VITE_*
# convention) — changing this after building the Docker image has no
# effect, you must rebuild.
VITE_API_URL="https://baraka-api.edvo.uz/api"
